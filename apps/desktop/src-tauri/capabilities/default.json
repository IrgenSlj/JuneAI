{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Permissions granted to the main June window. Phase 1 shipped the minimum surface needed to load the UI; Phase 3 added shell-open for the Ollama installer URL; Phase 4 adds tray, autostart, global-shortcut, and window-state.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:tray:default",
    "shell:allow-open",
    "notification:default",
    "global-shortcut:default",
    "autostart:default",
    "window-state:default"
  ]
}
