# June v0.3 — Execution Plan

> **MERGED (2026-07-26) into `v0.3-development-plan.md`, which was itself
> SUPERSEDED (2026-08-18) by
> [`v0.4-development-plan.md`](v0.4-development-plan.md).**
>
> Two hops from the current plan. Nothing here is live.
>
> Splitting *what and why* from *in what order* produced two documents that
> disagreed with each other inside a day. There is now one plan of record, and it
> holds the phases, slices, and acceptance criteria that used to live here.
>
> This file is kept in place, not deleted, because ADR 0030 references it by
> path and ADRs are append-only — the same convention
> [`../archive/README.md`](../archive/README.md) applies to every superseded
> plan. The full prior contents are in git history.

**Phase 4 (the MCP server), referenced from ADR 0030, is now section 3 of the
consolidated plan.**
